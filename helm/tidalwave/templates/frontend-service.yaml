apiVersion: v1
kind: Service
metadata:
  name: {{ include "tidalwave.fullname" . }}-frontend
  labels:
    {{- include "tidalwave.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tidalwave.frontendSelectorLabels" . | nindent 4 }}
